import React from 'react';

const Hero = () => {
    return (
        <section className="pb-hero" id="home">
            <div className="pb-container">
                <div className="hero-content">
                    <h1 className="hero-title">Generate Smart Interview Reports in Seconds</h1>
                    <p className="hero-subtitle">Upload, describe, and let AI do the heavy lifting for you.</p>
                </div>
            </div>
        </section>
    );
};

export default Hero;
